{
  "name": "bulmapress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-clean": "rimraf css",
    "build-sass": "node-sass --output-style expanded --source-map true bulmapress.sass css/bulmapress.css --watch",
    "build": "npm run build-clean && npm run build-sass"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.7.2",
    "rifraf": "^2.0.3"
  },
  "devDependencies": {
    "bulma": "^0.6.1"
  }
}
